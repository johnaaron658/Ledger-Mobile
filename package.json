{
  "name": "ledger-mobile",
  "private": true,
  "workspaces": [
    "core",
    "engine",
    "frontend",
    "mobile"
  ],
  "scripts": {
    "test": "npm run test --workspaces --if-present"
  }
}
