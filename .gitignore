node_modules/
*/dist/
__pycache__/
*.pyc

# Golden-output fixtures: regenerable from the dashboard repo's
# tools/capture-fixtures.py + a journal, and they contain real financial
# data.
engine/test/golden/

# Mobile-app design and implementation notes — scratch, kept out of history,
# copied over from the dashboard repo for a fresh session's context. Same
# convention as dashboard/.gitignore.
MOBILE_APP*.md
