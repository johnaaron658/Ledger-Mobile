<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation|density"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"></meta-data>
        </provider>
    </application>

    <!-- Permissions -->

    <!-- Deliberately NO android.permission.INTERNET (removed from the
         template Capacitor's `cap add android` generates by default) —
         MOBILE_APP.md §9.2/§6 item 7: the app is offline-first by design,
         and none of the 5 plugins this project uses (filesystem, share,
         file-picker, app, biometric-auth) need network access. This is
         also a genuinely user-visible property on Android: an app with no
         INTERNET permission cannot make network calls even if a future
         dependency tried to, and Android surfaces "no internet access"
         in the app's permission listing.
         Caveat: `npx cap run android -l` (live-reload against the Vite dev
         server) needs this permission back temporarily — re-add it locally
         for that workflow, but don't commit it back. `cap sync`/`cap
         update` do not regenerate this file, so this edit is stable across
         syncs. -->
</manifest>
