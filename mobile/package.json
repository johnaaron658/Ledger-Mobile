{
  "name": "mobile",
  "private": true,
  "version": "0.0.0",
  "description": "Capacitor shell wrapping frontend/dist (Phase 5, VITE_LOCAL_ENGINE=1) — see MOBILE_APP_IMPLEMENTATION.md §6.",
  "scripts": {
    "build:web": "cd ../frontend && VITE_LOCAL_ENGINE=1 npm run build",
    "sync": "npm run build:web && npx cap sync",
    "doctor": "npx cap doctor",
    "open:android": "npx cap open android"
  },
  "dependencies": {
    "@aparajita/capacitor-biometric-auth": "^10.0.0",
    "@capacitor/android": "^8.5.2",
    "@capacitor/app": "^8.1.1",
    "@capacitor/core": "^8.5.2",
    "@capacitor/filesystem": "^8.1.3",
    "@capacitor/ios": "^8.5.2",
    "@capacitor/share": "^8.0.2",
    "@capawesome/capacitor-file-picker": "^8.1.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.5.2"
  }
}
